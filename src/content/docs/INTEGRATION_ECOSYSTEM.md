# Integration Ecosystem

Third-party provider command work now belongs to the standalone `orbit` repo.

Use this current operator entry point:

```bash
orbit list --json
orbit <provider> --help
```

SI remains the owner for SI runtime wrappers and internal runtime workflows. Do not treat old SI orbit-catalog examples as current executable SI commands.
